import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Websites' };

export default function WebsitesPage() {
  return <h1 className="text-2xl font-semibold tracking-tight">Websites</h1>;
}
